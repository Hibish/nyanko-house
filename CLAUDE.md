# にゃんこハウス (Nyanko House) — プロジェクト概要

## 概要
猫をテーマにしたHTML単一ファイルのカジュアルゲーム。
マージ＆放置ゲームで、猫をガチャで召喚・進化させてコースに配置しポイントを稼ぐ。

## 技術スタック
- 純粋なHTML（単一ファイル：index.html）
- GitHub (`Hibish/nyanko-house`) + Vercel（pushで自動デプロイ）
- コーディング不要のワークフロー：deploy.html（モバイル向けデプロイUI）

## ファイル構成
- `index.html` — ゲーム本体（全コードここに集約）
- `deploy.html` — モバイルからの指示→Claude API→GitHub APIでコミット・デプロイするUI

## ゲームの主な機能
- ガチャ召喚（アニメーション・レアフラッシュ演出）
- ドラッグ＆ドロップでグリッド/コースに猫を配置（タッチ対応）
- 二重通貨：🥫猫缶＋Pポイント
- ショップ（スピードブースト、ラッキーチャーム、グリッド拡張）
- ログインボーナス、報酬広告クールダウン
- 図鑑（Zukan）タブ

## 対象デバイス
iPhone SE3（375×667px）を最小基準とするPWA。

## 重要な実装メモ
- iOS Safariのビューポート：`window.innerHeight` → CSS変数 ＋ `overflow:hidden` on body（`100vh`/`100dvh`は使わない）
- コースへの配置はドラッグ＆ドロップのみ（専用ボタンなし）
- 通貨コストは固定（インフレ方式なし）
- GitHubへの直接上書き方式（削除→再アップロードしない）

## 今後の予定
- AdSense申請・広告統合（報酬広告のクールダウン機構は実装済み）
- モバイルワークフローを使った継続的な改善
